<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
    <style type="text/css">
        body{
            font-size: 8px;
        }
    </style>
</head>
<body>
    <div>
        <table>
            @foreach (var contrato in @Model)
            {
                <tr>
                    <td>@contrato.Tipo</td>
                </tr>
            }
        </table>
    </div>
</body>
</html>